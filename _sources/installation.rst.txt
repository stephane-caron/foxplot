************
Installation
************

You can install the ``foxplot`` command-line tool from PyPI:

.. code:: bash

    pip install foxplot

Add the ``--user`` parameter for a user-only installation.
